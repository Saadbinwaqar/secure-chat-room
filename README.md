uses rsa 256 encryption for confidentiality

digital signatures and hashing for integrity checking

one machine acts as server while multiple machines connect to it as clients using socket programming

all machines must be connected to the same network 

Host variable of Server should be 0.0.0.0 while in clients it should be the ip address of the server machine
